{
    "name": "pastebin",
    "version": "1.0.0",
    "private": true,
    "description": "Offline pastebin web application using HTML, CSS, vanilla JavaScript, and IndexedDB.",
    "scripts": {
        "dev": "live-server src --host=127.0.0.1 --port=4173 --no-browser",
        "start": "npm run dev"
    }
}
